// in this is have to hit 2 api: send message and suggest message
// use public url to send message, check is user is accepting message or not
// the respoce we get from AI will be shown in streams, also put loading while ai is thinking 
// useCompletion hook will be used for this

import React from 'react'

const profile = () => {
  return (
    <div>profile</div>
  )
}

export default profile